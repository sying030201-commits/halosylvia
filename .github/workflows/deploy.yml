name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ] # 如果你的主分支叫 master，请改为 master

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/node-versions@v4
        with:
          node-version: 20

      - name: Install and Build
        run: |
          npm install
          npm run build

      - name: Deploy
        uses: jamesives/github-pages-deploy-action@v4
        with:
          folder: dist # Vite 编译后的文件夹叫 dist
          branch: gh-pages # 部署到这个分支
